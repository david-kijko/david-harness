# Peep v2.1 Re-judge — Hephaestus Verdict

## Summary
- Net score (all 5 benchmarks): +2 (with_skill wins B2 and B4; B1, B3, and B5 tie; no baseline wins)
- Consistent outperformance: Yes
- Recommendation: SHIP

## Benchmark 2 — fault-localization (re-judged)
- Correct answer: The defective region is `src/promo.py`, where `PercentOff` stores `_applied` state and `apply()` returns the undiscounted amount once that flag is true (`benchmarks/bench2-fault-loc/prompt.md:40-49`). The shown test constructs a fresh promo inline (`benchmarks/bench2-fault-loc/prompt.md:5-10`), so the `~1 in 8` xdist/shared-fixture explanation is not proven by the prompt and must be treated as a hidden-state hypothesis, not a fact.
- with_skill v2.1 vs baseline: with_skill v2.1 is better. It explicitly verifies that the prompt constructs a fresh `PercentOff` (`benchmarks/bench2-fault-loc/with_skill/output.md:24-26`), marks fixture/shared-instance explanations as unsupported (`benchmarks/bench2-fault-loc/with_skill/output.md:28-38`), refutes the single-test double-apply path (`benchmarks/bench2-fault-loc/with_skill/output.md:40-42`), and still localizes the wrong value to the `_applied` short-circuit (`benchmarks/bench2-fault-loc/with_skill/output.md:75-81`, `benchmarks/bench2-fault-loc/with_skill/output.md:138-146`). The baseline correctly notices that the test as written should not see `_applied == True` (`benchmarks/bench2-fault-loc/baseline/output.md:38-39`), but then states that intermittency “therefore comes from instance sharing” (`benchmarks/bench2-fault-loc/baseline/output.md:38-41`) and presents the 1/8 worker-collision story as the mechanism (`benchmarks/bench2-fault-loc/baseline/output.md:55-69`) rather than as unverified hidden context.
- with_skill v2.1 vs v2.0 (iteration outcome): IMPROVED. v2.0 said the intermittency hypothesis was “CONFIRMED in mechanism” while admitting it could not name the polluting sibling test (`benchmarks/bench2-fault-loc/with_skill/output.v2.0.md:68-70`) and ended by asserting the xdist collision story as fact (`benchmarks/bench2-fault-loc/with_skill/output.v2.0.md:160-162`); v2.1 demotes that same story to a hypothesis requiring full-suite/conftest evidence (`benchmarks/bench2-fault-loc/with_skill/output.md:141-146`).
- Winner: with_skill
- Discriminating finding: with_skill v2.1 says xdist cross-process shared state has no prompt evidence and is contradicted by standard xdist semantics (`benchmarks/bench2-fault-loc/with_skill/output.md:32-34`), while the baseline still gives the shared-worker collision mechanism as the explanation (`benchmarks/bench2-fault-loc/baseline/output.md:55-69`).

## Benchmark 5 — greenfield (re-judged)
- Correct answer: A strong plan should be handoff-ready for the spec in `prompt.md`: create a Python CLI named `csv2jsonl` that reads CSV from stdin, writes JSON Lines to stdout, uses the header row as keys, handles quoted commas, is invokable as `csv2jsonl < input.csv > output.jsonl`, lists files/dependencies/tests/verification commands, and does not actually create files (`benchmarks/bench5-greenfield/prompt.md:3-10`). It should prefer stdlib `csv` + `json`, zero runtime dependencies, streaming row-by-row, a console-script entry point, quoted-comma tests, and fresh-clone install/test/smoke commands.
- with_skill v2.1 vs baseline: tie. with_skill v2.1 fixes the original stdlib-semantics problem by checking `DictReader` extras and missing-cell behavior at runtime (`benchmarks/bench5-greenfield/with_skill/output.md:88-95`) and recording the corrected `restkey`/`restval` facts in the gap table (`benchmarks/bench5-greenfield/with_skill/output.md:98-101`); it also gives concrete implementation skeletons for `_core.py`, `cli.py`, and `pyproject.toml` (`benchmarks/bench5-greenfield/with_skill/output.md:400-470`), plus install/test/smoke commands (`benchmarks/bench5-greenfield/with_skill/output.md:283-292`). The baseline remains at least as handoff-ready: it states the whole spec succinctly (`benchmarks/bench5-greenfield/baseline/output.md:5-14`), rejects heavy dependencies while choosing stdlib streaming (`benchmarks/bench5-greenfield/baseline/output.md:20-38`), correctly describes `DictReader` ragged-row defaults and chooses `restkey="_extra"` (`benchmarks/bench5-greenfield/baseline/output.md:47-50`, `benchmarks/bench5-greenfield/baseline/output.md:104-108`), provides a compact tool skeleton (`benchmarks/bench5-greenfield/baseline/output.md:94-127`), and gives exact fresh-clone commands plus expected smoke output (`benchmarks/bench5-greenfield/baseline/output.md:192-222`). with_skill v2.1 is more source-checked, but it also expands into non-spec policy (BOM, SIGPIPE, row-length errors) (`benchmarks/bench5-greenfield/with_skill/output.md:45-57`, `benchmarks/bench5-greenfield/with_skill/output.md:104-105`) and has a minor surface inconsistency where it says “Total: 7 files” (`benchmarks/bench5-greenfield/with_skill/output.md:160-161`) but the created-surface table lists eight rows including `_core.py` (`benchmarks/bench5-greenfield/with_skill/output.md:222-229`).
- with_skill v2.1 vs v2.0 (iteration outcome): IMPROVED. v2.0 made the exact confident-wrong claim the original judge penalized — “extra cells are dropped” and “missing keys map to empty string” as `DictReader` defaults (`benchmarks/bench5-greenfield/with_skill/output.v2.0.md:47-50`) — while v2.1 explicitly refutes both defaults and chooses explicit `restkey`/`restval` handling (`benchmarks/bench5-greenfield/with_skill/output.md:98-101`, `benchmarks/bench5-greenfield/with_skill/output.md:410-417`). v2.1 also adds a handoff-ready implementation sketch (`benchmarks/bench5-greenfield/with_skill/output.md:398-470`) that v2.0 did not provide.
- Winner: tie
- Discriminating finding: The iteration’s decisive fix is that v2.1 turns the old false `DictReader` default claim into verified facts and corresponding code (`benchmarks/bench5-greenfield/with_skill/output.md:88-101`, `benchmarks/bench5-greenfield/with_skill/output.md:410-417`), but the baseline already had a compact correct skeleton and commands (`benchmarks/bench5-greenfield/baseline/output.md:94-127`, `benchmarks/bench5-greenfield/baseline/output.md:192-222`).
- Cost-benefit: The GAP IDENTIFICATION step was not pure ceremony: it caught real errors in `DictReader` defaults (`benchmarks/bench5-greenfield/with_skill/output.md:98-101`) and `json.dumps(..., ensure_ascii=True)` (`benchmarks/bench5-greenfield/with_skill/output.md:90-102`), directly addressing the original failure mode. The cost is still high for a tiny CLI: some of the 25 tool uses fed extra-scope policies such as BOM and SIGPIPE handling (`benchmarks/bench5-greenfield/with_skill/output.md:104-105`, `benchmarks/bench5-greenfield/with_skill/output.md:355-359`) rather than making the core handoff materially better than the baseline.

## Carried over (unchanged from JUDGE-VERDICT.md)
- B1 patch-equivalence: tie
- B3 code-qa: tie
- B4 brownfield: with_skill win

## Overall Reasoning
with_skill now clears the consistency bar: it is not worse than baseline on any of the five benchmarks, with wins on B2 and B4 and ties on B1, B3, and B5. The targeted B2 reachability gate fixed a real reasoning defect by separating the proven `src/promo.py` state bug from the unproven xdist/shared-fixture trigger, and the B5 gap step fixed the concrete `csv.DictReader` falsehood that caused the previous loss.

Recommendation: SHIP, with one follow-up tuning note. The B5 gap-research requirement earned its keep as a correctness guard, but the template should eventually constrain research-triggered edge-case expansion so a tiny greenfield plan does not grow BOM/SIGPIPE/ragged-row policy unless those choices are explicitly framed as optional or directly necessary for the user-visible spec.
