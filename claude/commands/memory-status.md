Show the full status of the kijko-memory episodic memory system.

Run both commands:
```
kijko-memory stats
kijko-memory health
```

Report all layers (L0 messages, L1 summaries, L2 events, L3 assertions, embeddings), the auto-ingest timer state, and when the last successful ingest happened.
