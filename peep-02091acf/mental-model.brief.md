Use case: infographic-diagram
Asset type: engineering change-boundary mental model for CLI-03 authentication
Primary request: Draw a crisp architecture flow showing a terminal user entering the four new oclif commands, an auth command runtime resolving one profile/API target, and a focused AuthService orchestrating the already-existing HTTP client and OS credential store. For register/password login, show password input entering only from a hidden TTY prompt or warned stdin, then an unauthenticated auth POST returning a session cookie held inside a dashed “process memory only” capsule. That cookie must travel to exactly one PAT issuance POST and terminate there. The issued PAT travels in memory to GET /api/me, then into the OS credential store; a bold barrier states “never stdout, never config.” For token import, show stdin PAT going through GET /api/me and GET capabilities before optional store. For status, show credential resolution to both identity and capabilities, then sanitized human/JSON output. For logout, show remote Bearer DELETE current token before local deletion, with a split result where remote failure plus local success yields exit 9.
Style/medium: precise flat systems diagram, white background, restrained navy/teal/orange palette, readable monospace labels, no decorative characters.
Composition/framing: left-to-right main flow with four command swimlanes; existing shared components in gray boxes, new CLI-03 components in teal boxes, external backend endpoints in navy boxes, secret-bearing transient edges in orange dashed lines. A thin red crossed-out box below labels the explicitly rejected browser `/api/auth/logout`, password argv/env, plaintext config, and device flow.
Text (verbatim where used): “process memory only”, “never stdout, never config”, “exit 9 partial”, “OS credential store”, “GET /api/me/capabilities”.
Invariants the picture must encode: the session cookie ends at PAT issuance and never reaches disk; the raw PAT is never connected to output/config; token import stores only after both validations; logout deletes locally even after revoke failure; status derives limits/features from capabilities rather than a client plan matrix; existing HttpClient/OutputRenderer/CredentialStore boundaries are reused rather than duplicated.
What the picture must not imply: no backend or frontend code changes; no browser logout call; no password flag or password environment variable; no cookie jar or persisted cookie; no PAT-to-PAT minting; no device authorization flow; no plaintext credential fallback; no printing of the PAT; no edits outside the listed CLI auth/test surface.
Constraints: technical accuracy over visual flourish; arrows must be directional; secret edges clearly distinct; all labels legible; no source code snippets; no logos; no watermark.
